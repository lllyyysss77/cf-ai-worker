{
	"name": "cf-ai-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "node --test tests/*.test.mjs"
	},
	"devDependencies": {
		"esbuild": "^0.28.1",
		"wrangler": "^4.61.1"
	}
}
