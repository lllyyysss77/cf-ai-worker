{
	"name": "cf-ai-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"start": "wrangler dev",
		"test": "node --test tests/*.test.mjs"
	},
	"devDependencies": {
		"wrangler": "^4.61.1"
	}
}
